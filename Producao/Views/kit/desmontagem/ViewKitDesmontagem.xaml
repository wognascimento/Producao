<UserControl x:Class="Producao.Views.kit.desmontagem.ViewKitDesmontagem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Producao.Views.kit.desmontagem" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>


        <StackPanel Grid.RowSpan="1" Margin="5" HorizontalAlignment="Left">

            <telerik:RadComboBox
                x:Name="cbSiglaShopping"
                Width="260"
                EmptyText="Shopping"
                IsEditable="True"
                IsFilteringEnabled="True"
                OpenDropDownOnFocus="True"
                DisplayMemberPath="cliente"
                SelectedValuePath="num_os"
                ItemsSource="{Binding Siglas}"
                SelectedItem="{Binding Sigla, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                SelectionChanged="OnSiglaSelectionChanged" Margin="0,0,2,2" IsTextSearchEnabled="True" StaysOpenOnEdit="True" CanAutocompleteSelectItems="False" CanKeyboardNavigationSelectItems="False" UpdateSelectionOnLostFocus="False" UpdateTextOnLostFocus="False"/>

        </StackPanel>

        <telerik:RadGridView 
            x:Name="osKit"
            Grid.Row="1"
            AutoGenerateColumns="False"
            CanUserInsertRows="True"
            GroupRenderMode="Flat"
            NewRowPosition="Bottom"
            ShowGroupPanel="False"
            ItemsSource="{Binding OsKits}"
            SelectedItem="{Binding OsKit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            AddingNewDataItem="RadGridView_AddingNewDataItem"
            RowValidating="RadGridView_RowValidating"
            RowValidated="RadGridView_RowValidated"
            IsReadOnly="False">
            <telerik:RadGridView.Columns>

                <telerik:GridViewDataColumn Header="DATA EMISSAO" DataMemberBinding="{Binding data_emissao}" DataFormatString="{}{0:dd/MM/yyyy}"/>

                <telerik:GridViewDataColumn Header="SOLICITANTE" DataMemberBinding="{Binding solicitante}"/>

                <telerik:GridViewDataColumn Header="CONCLUIR ATE" DataMemberBinding="{Binding concluir_ate}" DataFormatString="{}{0:dd/MM/yyyy}"/>

                <telerik:GridViewDataColumn Header="ATENDENTE" DataMemberBinding="{Binding atendente}"/>

                <telerik:GridViewDataColumn Header="DATA SOLICITACAO" DataMemberBinding="{Binding data_solicitacao}" DataFormatString="{}{0:dd/MM/yyyy}"/>

                <telerik:GridViewDataColumn Header="NOITE DE MONTAGEM" DataMemberBinding="{Binding noite_montagem}"/>

                <telerik:GridViewDataColumn Header="OBSERVAÇÃO" DataMemberBinding="{Binding obs_de_envio}"/>

                <telerik:GridViewDataColumn Header="CONTROLE" DataMemberBinding="{Binding os}"/>

                <telerik:GridViewColumn Header="DETALHES">
                    <telerik:GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <telerik:RadButton 
                                Content="DETALHES" 
                                Command="{Binding DataContext.RowDetalhesCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                CommandParameter="{Binding}" Margin="0,0,2,2"/>
                        </DataTemplate>
                    </telerik:GridViewColumn.CellTemplate>
                </telerik:GridViewColumn>

            </telerik:RadGridView.Columns>

        </telerik:RadGridView>


    </Grid>
</UserControl>
