<UserControl x:Class="Producao.Views.OrdemServico.Produto.BaixaOrdemServicoProduto"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Producao.Views.OrdemServico.Produto"
             xmlns:app="clr-namespace:Producao" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             Loaded="UserControl_Loaded" 
             Unloaded="UserControl_Unloaded">

    <UserControl.Resources>
        <app:ConverterNumber x:Key="ConverterNumber"/>
        <app:ConverterBoolen x:Key="ConverterBoolen"/>
    </UserControl.Resources>


    <Grid>

        <telerik:RadGridView
            x:Name="itensGrid"
            AutoGenerateColumns="False"
            IsFilteringAllowed="True"
            ShowGroupPanel="False"
            GroupRenderMode="Flat"
            ItemsSource="{Binding Itens}"
            CanUserResizeColumns="True" 
            PreviewMouseRightButtonDown="ItensGrid_PreviewMouseRightButtonDown"
            RowEditEnded="RadGridView_RowEditEnded" >

            <telerik:RadGridView.ContextMenu>
                <ContextMenu Style="{x:Null}">

                    <MenuItem Header="Cancelar" Click="OnCancelarClick"/>

                </ContextMenu>
            </telerik:RadGridView.ContextMenu>
            
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="num_os_produto" DataMemberBinding="{Binding num_os_produto, Converter={StaticResource ConverterNumber}}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="num_os_servico" DataMemberBinding="{Binding num_os_servico, Converter={StaticResource ConverterNumber}}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="recebido_setor_data" DataMemberBinding="{Binding recebido_setor_data, StringFormat={}{0:dd/MM/yyyy}}"/>
                <telerik:GridViewDataColumn Header="concluida_os_data" DataMemberBinding="{Binding concluida_os_data, StringFormat={}{0:dd/MM/yyyy}}"/>
                <telerik:GridViewDataColumn Header="situacao" DataMemberBinding="{Binding situacao}" IsReadOnly="True"/>
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>

    </Grid>
    

</UserControl>
