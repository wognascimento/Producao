<UserControl x:Class="Producao.Views.OrdemServico.Requisicao.RequisicaoMaterialEmitir"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="250"
             Unloaded="UserControl_Unloaded">

    <Grid Margin="5" VerticalAlignment="Center" HorizontalAlignment="Center" Height="280" Width="240">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="NÚMERO DE SERVIÇO"/>
            <telerik:RadWatermarkTextBox TextAlignment="Center" PreviewKeyDown="OnPreviewKeyDown"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="SETOR"/>
            <telerik:RadWatermarkTextBox Text="{Binding TGlobal.descricao_setor}" IsEnabled="False" TextAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="DATA"/>
            <telerik:RadWatermarkTextBox x:Name="txtData" IsEnabled="False" TextAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Text="EMITENTE"/>
            <telerik:RadWatermarkTextBox x:Name="txtEmitente" IsEnabled="False" TextAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock Text="NÚMERO DA REQUISIÇÃO"/>
            <telerik:RadWatermarkTextBox TextAlignment="Center" MouseDoubleClick="OnMouseDoubleClick"/>
        </StackPanel>
    </Grid>
</UserControl>
