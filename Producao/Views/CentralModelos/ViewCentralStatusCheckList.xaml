<UserControl x:Class="Producao.Views.CentralModelos.ViewCentralStatusCheckList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Producao.Views.CentralModelos"
             xmlns:app="clr-namespace:Producao"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d"
             Language="pt-BR"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Loaded="UserControl_Loaded"
             Unloaded="UserControl_Unloaded">

    <UserControl.Resources>
        <app:ConverterNumber x:Key="ConverterNumber"/>
    </UserControl.Resources>

    <Grid>
        <telerik:RadGridView
            x:Name="dgTabela"
            AutoGenerateColumns="False"
            ItemsSource="{Binding Itens}"
            FontSize="9"
            ShowGroupPanel="False"
            IsFilteringAllowed="True">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="MODELO" Width="90">
                    <telerik:GridViewDataColumn.CellTemplate>
                        <DataTemplate>
                            <telerik:RadButton
                                Content="Gerar"
                                Padding="8,2"
                                Click="OnGerarModeloClick" Margin="0,0,2,2"/>
                        </DataTemplate>
                    </telerik:GridViewDataColumn.CellTemplate>
                </telerik:GridViewDataColumn>
                <telerik:GridViewDataColumn Header="data_de_expedicao" DataMemberBinding="{Binding data_de_expedicao}" DataFormatString="{}{0:dd/MM/yyyy}"/>
                <telerik:GridViewDataColumn Header="sigla" DataMemberBinding="{Binding sigla}"/>
                <telerik:GridViewDataColumn Header="tema" DataMemberBinding="{Binding tema}"/>
                <telerik:GridViewDataColumn Header="local_shoppings" DataMemberBinding="{Binding local_shoppings}"/>
                <telerik:GridViewDataColumn Header="planilha" DataMemberBinding="{Binding planilha}"/>
                <telerik:GridViewDataColumn Header="descricao_completa" DataMemberBinding="{Binding descricao_completa}"/>
                <telerik:GridViewDataColumn Header="orientacao_producao" DataMemberBinding="{Binding obs}" Width="SizeToHeader"/>
                <telerik:GridViewDataColumn Header="item_memorial" DataMemberBinding="{Binding item_memorial}" Width="SizeToHeader"/>
                <telerik:GridViewDataColumn Header="laco" DataMemberBinding="{Binding laco}" Width="SizeToHeader"/>
                <telerik:GridViewDataColumn Header="obs_iluminacao" DataMemberBinding="{Binding obs_iluminacao}" Width="SizeToHeader"/>
                <telerik:GridViewDataColumn Header="id_modelo" DataMemberBinding="{Binding id_modelo, Converter={StaticResource ConverterNumber}}" Width="65"/>
                <telerik:GridViewDataColumn Header="qtde_compl" DataMemberBinding="{Binding qtde_compl}" DataFormatString="{}{0:N0}" Width="60"/>
                <telerik:GridViewDataColumn Header="status" DataMemberBinding="{Binding status}" Width="60"/>
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>

</UserControl>
