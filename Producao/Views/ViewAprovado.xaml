<UserControl x:Class="Producao.Views.ViewAprovado"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Loaded="UserControl_Loaded"
             Unloaded="UserControl_Unloaded">

    <Grid>
        <telerik:RadGridView
            x:Name="itens"
            ItemsSource="{Binding Aprovados}"
            SelectedItem="{Binding Aprovado, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            AutoGenerateColumns="False"
            CanUserFreezeColumns="True"
            CanUserReorderColumns="True"
            CanUserResizeColumns="True"
            ClipboardCopyMode="Cells,Header"
            FrozenColumnCount="12"
            IsFilteringAllowed="True"
            RowValidated="itens_RowValidated"
            SelectionMode="Extended"
            SelectionUnit="FullRow"
            ShowGroupPanel="False">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="Evento" DataMemberBinding="{Binding tipo_evento}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Ordem" DataMemberBinding="{Binding ordem}" DataFormatString="{}{0:N2}"/>
                <telerik:GridViewDataColumn Header="Ordem Sigla Serviço" DataMemberBinding="{Binding ordem_sigla_serv}" DataFormatString="{}{0:N0}"/>
                <telerik:GridViewDataColumn Header="Nivel" DataMemberBinding="{Binding nivel}"/>
                <telerik:GridViewDataColumn Header="Data Aprovado" DataMemberBinding="{Binding data_aprovado}" DataFormatString="{}{0:dd/MM/yyyy}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Data Expedição" DataMemberBinding="{Binding data_de_expedicao}" DataFormatString="{}{0:dd/MM/yyyy}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Meta Produção" DataMemberBinding="{Binding meta_producao}" DataFormatString="{}{0:dd/MM/yyyy}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Sigla" DataMemberBinding="{Binding sigla}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Sigla Serviço" DataMemberBinding="{Binding sigla_serv}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Rede" DataMemberBinding="{Binding rede}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Nome" DataMemberBinding="{Binding nome}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Tema" DataMemberBinding="{Binding tema}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Projeto Novo" DataMemberBinding="{Binding projeto_novo}"/>
                <telerik:GridViewDataColumn Header="Obs Especial" DataMemberBinding="{Binding obs_especial}"/>
                <telerik:GridViewDataColumn Header="Resp Cliente" DataMemberBinding="{Binding resp_cliente}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="UF" DataMemberBinding="{Binding est}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Cidade" DataMemberBinding="{Binding cidade}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Memo Resp" DataMemberBinding="{Binding memo_resp}"/>
                <telerik:GridViewDataColumn Header="Memo Data" DataMemberBinding="{Binding memo_data}" DataFormatString="{}{0:dd/MM/yyyy}"/>
                <telerik:GridViewDataColumn Header="Cronog Data" DataMemberBinding="{Binding cronog_data}" DataFormatString="{}{0:dd/MM/yyyy}"/>
                <telerik:GridViewDataColumn Header="Cronog Resp" DataMemberBinding="{Binding cronog_resp}"/>
                <telerik:GridViewDataColumn Header="Rel Inflamabilidade" DataMemberBinding="{Binding rel_inflamabilidade}"/>
                <telerik:GridViewDataColumn Header="Data Rel Inflamabilidade" DataMemberBinding="{Binding data_rel_inflamabilidade}" DataFormatString="{}{0:dd/MM/yyyy}"/>
                <telerik:GridViewDataColumn Header="Meta Rel Inflamabilidade" DataMemberBinding="{Binding meta_rel_inflamabilidade}" DataFormatString="{}{0:dd/MM/yyyy}"/>
                <telerik:GridViewDataColumn Header="PA" DataMemberBinding="{Binding pa}"/>
                <telerik:GridViewDataColumn Header="Acabamento Construção" DataMemberBinding="{Binding acabamento_construcao}"/>
                <telerik:GridViewDataColumn Header="Acabamento Fibra" DataMemberBinding="{Binding acabamento_fibra}"/>
                <telerik:GridViewDataColumn Header="Acabamento Móveis" DataMemberBinding="{Binding acabamento_moveis}"/>
                <telerik:GridViewDataColumn Header="Laço" DataMemberBinding="{Binding laco}"/>
                <telerik:GridViewDataColumn Header="Cor Predominante" DataMemberBinding="{Binding cor_predominante}"/>
                <telerik:GridViewDataColumn Header="Iluminação" DataMemberBinding="{Binding mlamp_led}"/>
                <telerik:GridViewDataColumn Header="Obs Iluminação" DataMemberBinding="{Binding obs_iluminacao}"/>
                <telerik:GridViewDataColumn Header="Resp Memo Visual" DataMemberBinding="{Binding resp_memo_visual}"/>
                <telerik:GridViewDataColumn Header="Data Memo Visual" DataMemberBinding="{Binding data_memo_visual}" DataFormatString="{}{0:dd/MM/yyyy}"/>
                <telerik:GridViewDataColumn Header="Data Reunião Conceito" DataMemberBinding="{Binding data_reuniao_conceito}" DataFormatString="{}{0:dd/MM/yyyy}"/>
                <telerik:GridViewDataColumn Header="Resp Revisão Planta Base" DataMemberBinding="{Binding resp_revisao_planta_base}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Data Conclusão Revisão Planta Base" DataMemberBinding="{Binding data_revisao_planta_base}" DataFormatString="{}{0:dd/MM/yyyy}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Resp Planta Praça" DataMemberBinding="{Binding resp_planta_pca}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Prazo Planta Praça" DataMemberBinding="{Binding prazo_planta_pca}" DataFormatString="{}{0:dd/MM/yyyy}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Data Conclusão Planta Praça" DataMemberBinding="{Binding liberacao_planta_pca}" DataFormatString="{}{0:dd/MM/yyyy}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Divi Caminhão" DataMemberBinding="{Binding divi_caminhao}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Header="Resp Estruturas" DataMemberBinding="{Binding resp_estruturas}"/>
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>
</UserControl>
